import React, {Component} from 'react';

export default class EditItem extends Component{
	render(){
		let input;
		console.log(this.props.onSave);
		return();
	}
}

//After changing the logic in Item.js,this isn't necessary anymore.But I'll keep it as a record.